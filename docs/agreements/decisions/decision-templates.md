## Decision template

A formal template is forthcoming. Until then, log a decision where the work already lives: post it in the relevant Zulip channel with enough context for someone to understand what was decided and why, and make sure anyone it affects hears about it directly rather than finding out later (this is what we mean by radiating intent).

### Document approval 
Todo:

- Proposal guide / example